# AJSMGPT — Fix List

Open failures with root cause + fix. Status: ⬜ open · 🔄 in progress · ✅ fixed.

Empty — no evaluation has run yet. Populate once the eval harness (`plan.md` next steps) produces
real failures, same convention as V1: one numbered entry per distinct root cause, not per question.
